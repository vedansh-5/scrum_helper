{
    "name": "scrum-helper-deploy",
    "version": "1.0.0",
    "scripts": {
        "build": "node build.js",
        "deploy": "node deploy.js",
        "publish": "npm run build && npm run deploy"
    },
    "devDependencies": {
        "archiver": "^5.3.1",
        "chrome-webstore-upload": "^3.0.0",
        "fs-extra": "^11.1.0",
        "googleapis": "^126.0.1"
    }
}